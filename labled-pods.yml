   apiVersion: v1
   kind: Pod
   metadata:
     name: frontend-pod
     labels:
       app: myapp
       tier: frontend
       environment: production
   spec:
     containers:
     - name: nginx
       image: nginx:1.21
   ---
   apiVersion: v1
   kind: Pod
   metadata:
     name: backend-pod
     labels:
       app: myapp
       tier: backend
       environment: production
   spec:
     containers:
     - name: nginx
       image: nginx:1.21
   ---
   apiVersion: v1
   kind: Pod
   metadata:
     name: cache-pod
     labels:
       app: myapp
       tier: cache
       environment: development
   spec:
     containers:
     - name: redis
       image: redis:alpine
